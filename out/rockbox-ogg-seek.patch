--- a/lib/rbcodec/metadata/ogg.c
+++ b/lib/rbcodec/metadata/ogg.c
@@ -147,9 +147,18 @@
      */
 
     /* A page is always < 64 kB */
-    if (lseek(fd, -(MIN(64 * 1024, id3->filesize)), SEEK_END) < 0)
+    /* Отсчёт от конца файла (SEEK_END с отрицательным смещением) в этой
+     * сборке оставляет файл в состоянии «конец достигнут»: позиция
+     * показывается верной, а следующее чтение сразу возвращает ноль
+     * байт. Из-за этого последняя страница Ogg не находилась, серийный
+     * номер оставался нулевым и разбор считал файл битым — не играли ни
+     * Vorbis, ни Opus. Идём к той же точке прямо от начала файла. */
     {
-        return false;
+        off_t tail = MIN(64 * 1024, id3->filesize);
+        if (lseek(fd, id3->filesize - tail, SEEK_SET) < 0)
+        {
+            return false;
+        }
     }
 
     while (!eof) 
